@{
    Layout = "~/Shared/_Layout.cshtml";
    Page.Title = "Lessenrooster";

    if (IsPost)
    {
        string action = Request["action"];

        if (action.Equals("login") && Session["cursist"] == null)
        {
            Session["cursist"] = Administratix.Model.Cursist.GetCursistByCursistNummer(Convert.ToInt32( Request["Login-CursistNummer"]));
        }
        if (action.Equals("logout"))
        {
            Session["cursist"] = null;
        }



    }

    //System.Threading.Thread.CurrentThread.CurrentCulture = new System.Globalization.CultureInfo("nl-BE");

    System.Globalization.CultureInfo culture = new System.Globalization.CultureInfo("nl-BE");

    string message = "Alles gaat goed.";

    List<Administratix.BLL.LesDavinci> lessenrooster = new List<Administratix.BLL.LesDavinci>();
    if (Session["cursist"] != null)
    {
        Administratix.BLL.Cursist cursist = (Administratix.BLL.Cursist)Session["cursist"];
        //lessenrooster = Administratix.Model.LesDavinci.LesroosterTonen(Convert.ToInt32(cursistNr));
        
        //testdatum
        DateTime testDatum = new DateTime(2014, 1, 1);
        lessenrooster = Administratix.Model.LesDavinci.LesroosterTussenDatums(cursist.CursistNummer, testDatum, new DateTime(2015, 8, 1));

    }
    
    

}


<main>
    <div id="centerContainer">

        @*@if (Session["cursistNummer"] != null && Session["cursistNummer"] != "")
        {

        }*@

        <div class="containerBig">
            <div class="containerCalendarRow">
                <div class="containerCalendarTitle columnTiny">
                    Nr
                </div>
                <div class="containerCalendarTitle">
                    Datum
                </div>
                <div class="containerCalendarTitle">
                    Dag
                </div>
                <div class="containerCalendarTitle columnMed">
                    Docent
                </div>
                <div class="containerCalendarTitle">
                    Campus
                </div>
                <div class="containerCalendarTitle columnTiny">
                    Lokaal
                </div>
                <div class="containerCalendarTitle columnBig">
                    Module
                </div>
                <div class="containerCalendarTitle columnTiny">
                    Van
                </div>
                <div class="containerCalendarTitle columnTiny">
                    Tot
                </div>
            </div>

            @for (int i = 0; i < lessenrooster.Count; i++)
            {
                Administratix.BLL.LesDavinci les = lessenrooster[i];
                <div class="containerCalendarRow @(i%2 == 0 ? "":"shaded")">
                    <div class="containerCalendarColumn columnTiny">
                        @les.Cursusnummer
                    </div>
                    <div class="containerCalendarColumn">
                        @les.Datum.ToString("dd-MM-yyyy", culture)
                    </div>
                    <div class="containerCalendarColumn">
                        @les.Datum.ToString("dddd", culture)
                    </div>
                    <div class="containerCalendarColumn columnMed">
                        @les.Docent
                    </div>
                    <div class="containerCalendarColumn">
                        @les.Campus
                    </div>
                    <div class="containerCalendarColumn columnTiny">
                        @les.Lokaal
                    </div>
                    <div class="containerCalendarColumn columnBig">
                        @les.Module
                    </div>
                    <div class="containerCalendarColumn columnTiny">
                        @les.Aanvangsdatum.ToString("HH:mm", culture)
                    </div>
                    <div class="containerCalendarColumn columnTiny">
                        @les.Einddatum.ToString("HH:mm", culture)
                    </div>
                </div>

            }
            
        </div>



            @*@{
                if (lessenrooster.Count <= 0)
                {
                    <p>Geen lessenrooster gevonden.</p>
                }
                else
                {
                    <div class="containerBig">
                        <table>
                            <tr>
                                <th class="containerCalendarTitle">Cursusnummer</th>
                                <th class="containerCalendarTitle">Weekdag</th>
                                <th class="containerCalendarTitle">Datum</th>
                                <th class="containerCalendarTitle">Module</th>
                                <th class="containerCalendarTitle">Docent</th>
                                <th class="containerCalendarTitle">Lokaal</th>
                                <th class="containerCalendarTitle">Campus</th>
                                <th class="containerCalendarTitle">Van</th>
                                <th class="containerCalendarTitle">Tot</th>
                            </tr>
                            @for (int i = 0; i < lessenrooster.Count; i++)
                            {
                                Administratix.BLL.LesDavinci les = lessenrooster[i];
                                <tr>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Cursusnummer</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Dag</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Datum</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Module</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Docent</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Lokaal</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Campus</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Aanvangsdatum</td>
                                    <td class="@(i%2 == 0 ? "":"shaded")">@les.Einddatum</td>
                                </tr>
                            }
                        </table>
                    </div>
                }
}*@


                    </div>
</main>